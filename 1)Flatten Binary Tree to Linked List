class Solution:
    def flatten(self, root: Optional[TreeNode]) -> None:
        if not root:
            return
        left = root.left
        right = root.right
        root.left = None
        root.right = left
        node = root
        while node.right:
            node = node.right
        node.right = right
        self.flatten(root.right)
output:
